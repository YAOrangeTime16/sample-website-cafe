import React from 'react';
import Menu from './Menu';

const Header = () => (
  <header className={`header`}>
      <h1 className="title">Lorem Café</h1>
      <Menu />
  </header>
)

export default Header;